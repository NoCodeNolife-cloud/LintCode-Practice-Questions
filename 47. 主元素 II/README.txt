描述
给定一个整型数组，找到主元素，它在数组中的出现次数大于数组元素个数的三分之一。

数组中只有唯一的主元素

样例
样例 1：

输入：

nums = [99,2,99,2,99,3,3]
输出：

99
解释：

99出现了3次
样例 2：

输入：

nums = [1, 2, 1, 2, 1, 3, 3]
输出：

1
解释：

1出现了3次

挑战
要求时间复杂度为O(n)O(n)，空间复杂度为O(1)O(1)。
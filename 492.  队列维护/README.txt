描述
按链表实现队列。支持以下基本方法：

enqueue(item).将新元素放入队列中。
dequeue(). 将第一个元素移出队列，返回它。
样例
例1:

输入：
enqueue(1)
enqueue(2)
enqueue(3)
dequeue() // return 1
enqueue(4)
dequeue() // return 2
例2:

输入：
enqueue(10)
dequeue()// return 10